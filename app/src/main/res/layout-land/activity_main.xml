<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/android_gridview_example"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/textViewSocial">
        <GridView
            android:id="@+id/gridview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnWidth="150dp"
            android:layout_centerInParent="true"
            android:numColumns="4"
            android:stretchMode="spacingWidthUniform">
        </GridView>
    </RelativeLayout>
    <TextView
        android:id="@+id/textViewSocial"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/fontawesome_webfont"
        android:text="@string/Social_icons"
        android:textAlignment="center"
        android:textColor="@color/primary_text"
        android:layout_above="@id/textViewShare"
        android:textSize="@dimen/social_font_size" />
    <TextView
        android:id="@+id/textViewShare"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/small_margin"
        android:layout_marginBottom="@dimen/medium_margin"
        android:text="@string/Share"
        android:fontFamily="@font/roboto"
        android:textAlignment="center"
        android:textSize="@dimen/medium_font_size"
        android:layout_alignParentBottom="true"/>


</RelativeLayout>
